# MacOS Only
APPLE_ID=
APPLE_APP_SPECIFIC_PASSWORD=
APPLE_TEAM_ID=
SSH_USER_PWD=

# Global
GPG_KEY_ID=
GPG_PASSPHRASE=
# GitHub releases use the GitHub CLI credential store. Run `gh auth login` on
# each release VM; no GitHub token belongs in this file.
AFTER_PACK_LOC=

# Windows Authenticode signing via Azure Artifact Signing (Public Trust) — OPTIONAL.
# Zephus is not code-signing yet: builds are unsigned by default and these
# variables only take effect when ALL of them are set.
# Once per Windows release VM (as Administrator), run: npm run setup:win:artifact-signing
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_SUBSCRIPTION_ID=
AZURE_CLIENT_SECRET=
AZURE_ARTIFACT_SIGNING_ENDPOINT=
AZURE_ARTIFACT_SIGNING_ACCOUNT=
AZURE_ARTIFACT_SIGNING_PROFILE=
AZURE_ARTIFACT_SIGNING_PUBLISHER=
# Only the exact value 1 forces an unsigned Windows build (explicit opt-out).
SKIP_WIN_CODESIGN=0
# Optional overrides if Client Tools are installed in a nonstandard location:
# AZURE_ARTIFACT_SIGNING_SIGNTOOL_PATH=
# AZURE_ARTIFACT_SIGNING_DLIB_PATH=
